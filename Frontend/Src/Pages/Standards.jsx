// Standards page — placeholder
// To expand: import { getStandardsData } from '../services/api'

const Standards = () => {
  return (
    <div style={{ padding: '5rem 2.5rem', textAlign: 'center' }}>
      <h1>Standards</h1>
    </div>
  );
};

export default Standards;
