// Contact page — placeholder
// To expand: import { getContactData, submitContactForm } from '../services/api'

const Contact = () => {
  return (
    <div style={{ padding: '5rem 2.5rem', textAlign: 'center' }}>
      <h1>Contact</h1>
    </div>
  );
};

export default Contact;
